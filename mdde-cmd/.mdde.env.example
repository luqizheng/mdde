# MDDE 环境变量配置文件
# 复制此文件为 .mdde.env 并根据需要修改

# MDDE 服务器地址 (会覆盖 config.toml 中的设置)
host=http://localhost:3000

# 容器名称 (可选)
# container_name=my-dev-env

# 调试端口 (可选)
# debug_port=5000

# 工作目录 (可选)
# workspace=./my-project

# 其他自定义环境变量
# custom_var=value

